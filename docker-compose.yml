services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PORT=8000
      - ENV=development
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
